---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: pivotalservices/pks-kubectl

inputs:
- name: pipelines-repo

run:
  path: pipelines-repo/tasks/deploy-pks-app/task.sh

params:
  UAA_URL:
  PKS_CLI_USERNAME:
  PKS_CLI_PASSWORD:
  APP_YAML_URL:
  VALUE_TO_REPLACE:
  REPLACEMENT_VALUE:
  K8S_NAMESPACE:
  K8S_CLUSTERNAME:    
