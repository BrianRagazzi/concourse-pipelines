---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: pivotalservices/pks-kubectl

inputs:
- name: pipelines-repo

run:
  path: pipelines-repo/tasks/deploy-pks-app/task.sh

params:
  PCF_PKS_API:
  PKS_CLI_USERNAME:
  PKS_CLI_PASSWORD:
  PKS_CLUSTER_NAME:
  YAML_SOURCE:
  NAMESPACE:
  UAA_URL:
  DEBUG:
